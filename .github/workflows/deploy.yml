name: Deploy to Render 🚀

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Render
        run: |
          curl -X POST https://api.render.com/deploy/srv-d5qhdu15pdvs7396ifa0 \
          -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}"
